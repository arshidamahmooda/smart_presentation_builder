# smart_presentation_builder


This app converts long text into a summarized PowerPoint presentation using **local ML models**.

### 🚀 Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
